#!/usr/bin/env node

'use strict';

var validNumbers = require('../../lib/utilities/validNumbers'),
	mergeSort = require('../../lib/sorts/bottomUpMergeSort'),
	input = validNumbers(process.argv);
	
if (input) {
	console.log(mergeSort(input));
}