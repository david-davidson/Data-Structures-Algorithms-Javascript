#!/usr/bin/env node

'use strict';

var validNumbers = require('../../lib/utilities/validNumbers'),
	insertionSort = require('../../lib/sorts/insertionSort');

// Sanitize input
if (validNumbers(process.argv)) {
	console.log(insertionSort(process.argv));
}