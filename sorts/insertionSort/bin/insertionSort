#!/usr/bin/env node

var validNumbers = require('../../../utilities/validNumbers');
var insertionSort = require('../lib/insertionSort').insertionSort;

// Sanitize input
if (!validNumbers()) {
	return false;
}
console.log(insertionSort(process.argv));