#!/usr/bin/env node

var validNumbers = require('../../../utilities/validNumbers');
var selectionSort = require('../lib/selectionSort').selectionSort;

// Sanitize input
if (!validNumbers()) {
	return false;
}
console.log(selectionSort(process.argv));