#!/usr/bin/env node

var validNumbers = require('../../../utilities/validNumbers');
var bubbleSort = require('../lib/bubbleSort').bubbleSort;

// Sanitize input
if (!validNumbers()) {
	return false;
}
console.log(bubbleSort(process.argv));